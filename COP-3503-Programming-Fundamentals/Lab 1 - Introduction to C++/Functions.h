#pragma once

#include <iostream>
#include <string>
#include <math.h>

using namespace std;

int quadraticRoot();

int gradeCalculator();

int cardValidator();
bool isValid(long long);
int sumOfDoubleEvenPlace(long long);
int sumOfOddPlace(long long);
int getDigit(int);
bool prefixMatched(long long, int);
int getSize(long long);
long long getPrefix(long long, int);
